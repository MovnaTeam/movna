/// A type of sport that can be practiced by a user.
enum Sport {
  hiking,
  running,
  biking,
  other,
}

// WARNING : do not modify the names of these fields, this would result in a
// breaking change as the Sport is stored by name in database.
